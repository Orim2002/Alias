[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "NODE_ENV=production node apps/server/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
